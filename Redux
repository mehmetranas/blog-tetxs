Redux kullnırken karmaşık objelerin state işlemlerinde Object.assign() metodu ile kopyalma yapılır fakat bu method type safe değildir. IState yapısını kolaylıkla manupüle edebilecek bir yapıdır. Bunun için tassign kütüphanesi kullanılabilr. tassign source objesinin içine yabancı bir property girişini engeller.
Redux kullnırken karşımıza çıkabilecek başka bir tehlike de reducer metodu içerisinde state objesinin propertilerinin değişime uğrama ihtimali. Malum state immutable olmalıdır. Bunun için de immutable.js kullanılabilir. Bu kütüphanedeki fromJs() ile ilk initial edilen yerde state immutable objeye dönüştürülür. fromJs() tüm propertylere ulaşır. Daha sonra reducer içinde tekrar map() kullnılarak erişim sağlanır. Fakat unutulmamalıdır ki immutable kütüphanesi kullanıldığı zaman tassign kullanılamaz, immutable kendi içerisinde farklı bir obje yapısı oluşturduğu için yine kendi içerisinde bulunun get ve set metotları ile propertylere ulaşabiliyoruz. Fakat tassign daha kullnışlı olduğunu sanıyorum.

ReduxDevTool: Redux için debug modu, çok başarılı.
